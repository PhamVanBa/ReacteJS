// import React from "react";

// function MySequance(number) {
//   // Will recalculate only when the number changes
//   const memoizedSequance = React.useMemo(() => getFibonacci(number), [number]);
// }

// export default MySequance;
